// Implement arrow function no variables

const noVar = () => "Hello there";

// Implement arrow function with one variable

const oneVar = param => `You entered ${param}`;

// Implement arrow function with two variables

const twoVar = (amount, fruit) => `You have ${amount} ${fruit}`;

// This arrow function should return the object literal:
// {fruit: "apple", colour: "red}

const objLiteral = () => ({ fruit: "apple", colour: "red" });

// What else can be improved for the first three functions?
